import React, { PropTypes } from 'react'
import Notation from './notation/index';

function Board(){
  let mode = 'preview';
  let id = 'board_01';
  let text = "# はじめまして"

  return <Notation mode={mode} id={id} text={text} />
}
// Board.propTypes = {};
export default Board
